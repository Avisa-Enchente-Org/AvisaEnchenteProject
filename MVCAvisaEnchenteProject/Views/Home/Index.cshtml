
@{
    ViewData["Title"] = "Página Inicial";
}

@if (User?.Identity?.IsAuthenticated ?? false)
{
    <div class="pt-3">
        <h4 class="text-primary">Bem Vindo, @User?.Identity?.Name.Split(" ")[0]!</h4>
    </div>
}

@section Scripts {
    <script src="~/js/pagina-inicial.js"></script>
}